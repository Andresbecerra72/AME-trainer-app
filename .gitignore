# Directorio de dependencias
node_modules

# Directorio de compilación de Next.js (siempre debe ignorarse)
.next/
out/

# Variables de entorno (archivos que contienen secretos o configuraciones locales)
.env
.env.local
.env.local.*
.env.development.local
.env.production.local

# Backups de entorno
.env.local.backup

# Archivos de registro (logs)
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Archivos y directorios de configuración del sistema operativo y editores
.DS_Store           # macOS
.idea/              # WebStorm/JetBrains
.vscode/            # VS Code (solo si no quieres compartir configuraciones específicas)
*.sw?               # Vim

# Archivos de TypeScript generados (si no usas tsconfig.json para salida custom)
*.tsbuildinfo

# Archivos de pruebas generados
coverage
